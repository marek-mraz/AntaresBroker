# Pinned, not "stable": clippy 1.98.0 (stable since 2026-08-18) runs out of
# memory on this workspace — two clippy-driver processes at 8 GB + 6 GB on a
# 16 GB runner, exit 143 with no diagnostic. 1.97 peaks at 2 GB. Re-try
# "stable" once a later release checks the workspace inside the budget.
[toolchain]
channel = "1.97.1"
components = ["clippy", "rustfmt"]
