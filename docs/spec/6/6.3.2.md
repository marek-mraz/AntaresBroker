---
clause: 6.3.2
title: Error Types
pages: '269'
status: implemented
evidence: 'antares-model/src/error.rs NgsiError::status (all 11 Table 6.3.2-1 rows asserted in status_mapping_matches_table_6_3_2_1);
  standard HTTP errors: 405+Allow (lib.rs router layer + not_found, TP 016_02), 411/413/414 (bounds.rs
  wall), 415 (per-handler content_type checks), 406 (negotiate::parse_accept)'
notes: >-
  LdContextNotAvailable restored to 504 (was 503 to please V1.8-era
  fixtures). errors/Conflict (409) is a 5.9.2.4-era extension beyond the
  table. The clause's second half, "implementations shall support the
  standard specific errors of HTTP bindings, such as the following", is an
  open list, and one entry beyond the five it names is Antares's own. A
  storage driver whose connection pool times out is answered 503 with
  Retry-After and no payload body, the way 6.3.4 answers 411 and 415; it
  claims no uri.etsi.org error type, because the specification names no
  error type for an overloaded server
  (crates/antares-api/src/negotiate.rs ApiError::Overloaded,
  crates/antares-api/tests/pool_overload_6_3_2.rs). A second entry is a
  refusal by the policy seam (ADR-0020): 403 with the ProblemDetails type
  urn:antares:error:AccessDenied and the engine's reason as detail, in this
  broker's own URN namespace because the table names no access-denied error
  and the built-in engine refuses nothing
  (crates/antares-api/src/policy.rs ACCESS_DENIED_TYPE,
  crates/antares-api/tests/policy_gate_seam.rs
  a_refusal_is_a_403_that_does_not_claim_an_etsi_error_type). An engine
  that narrows rather than refuses answers a normal status, and marks the
  answer with the response header Antares-Results-Restricted: true — an
  Antares name, not one under the NGSILD- prefix clause 6.3 defines
  (crates/antares-api/src/policy.rs RESTRICTED_HEADER,
  crates/antares-api/tests/policy_filter_5_7.rs
  the_restricted_header_reaches_every_read_that_was_narrowed). The engine
  the broker ships narrows nothing, so a conformance run never sees either.
robot:
- '046_01'
- '632_01'
---

6.3.2
Error Types
This clause associates API error types (which shall be contained in the response payload body) defined by clause 5.5.2
with HTTP status codes as shown in Table 6.3.2-1.
Table 6.3.2-1: Mapping of error types to HTTP status codes
Error Type
HTTP status
https://uri.etsi.org/ngsi-ld/errors/AlreadyExists
https://uri.etsi.org/ngsi-ld/errors/BadRequestData
https://uri.etsi.org/ngsi-ld/errors/InternalError
https://uri.etsi.org/ngsi-ld/errors/InvalidRequest
https://uri.etsi.org/ngsi-ld/errors/LdContextNotAvailable
https://uri.etsi.org/ngsi-ld/errors/NoMultiTenantSupport
https://uri.etsi.org/ngsi-ld/errors/NonexistentTenant
https://uri.etsi.org/ngsi-ld/errors/OperationNotSupported
https://uri.etsi.org/ngsi-ld/errors/ResourceNotFound
https://uri.etsi.org/ngsi-ld/errors/TooComplexQuery
https://uri.etsi.org/ngsi-ld/errors/TooManyResults

In addition, implementations shall support the standard specific errors of HTTP bindings, such as the following:
•
"Method Not Allowed" (405) which shall be raised when a client invokes a wrong HTTP verb over a resource.
Implementations shall provide the allowed HTTP methods as mandated by IETF RFC 7231 [3] in
section 6.5.5.
•
"Request Entity too large" (413) which shall be raised when the HTTP input data stream provided by a client
was too large i.e. too many bytes.
•
"Length required" (411) which shall be raised when an HTTP request provided by a client does not define the
"Content-Length" HTTP header.
•
"Unsupported Media Type" (415) which shall be raised when an HTTP request payload body (as per the
"Content-Type" header) it is not "application/json" nor "application/ld+json".
•
"Not Acceptable" (406) which shall be raised when the response media types that are acceptable by a client (as
per the "Accept" header) do not include or expand to "application/json" nor
"application/ld+json".
