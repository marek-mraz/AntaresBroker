---
clause: 6.3.14
title: Tenant specification
pages: '277'
status: implemented
evidence: 'negotiate.rs tenant_from (NGSILD-Tenant header, absent = default tenant) + echo_tenant on every
  response (incl. the NonexistentTenant 404 — 5.5.10); notifications carry NGSILD-Tenant for non-default
  tenants (notify.rs HTTP header + MQTT metadata). TPs 414_01 (incl. tenant-less CSR forward) + 5510_01
  green locally The header is read ONCE, at the outer wall (lib.rs tenant_exists_layer), through negotiate::single_header: it is not a list-type field, so repeated field lines cannot be joined (RFC 9110 clause 5.3) and a request carrying two names none. Before that the wall took HeaderMap::get''s first value for both of its decisions — the reserved-namespace refusal and the 5.5.10 existence check — while the handler''s tenant_from saw the repeat and answered 400, so a malformed request was answered NonexistentTenant 404 by the wall and the namespace guard inspected a name the operation would never have run on. echo_tenant_layer echoes nothing when the request names no Tenant. tests/tenant_header_6_3_14.rs (3 cases, two red first). No Robot TP: the suite''s HTTP keywords take headers as a dictionary, which cannot carry the same field twice. The names the broker mints for itself are refused by the TYPE a client''s name goes through (antares-model TenantId::new, is_reserved_str, tests tenant_rejects_the_names_the_broker_minted_for_itself) rather than by a wall a future path could forget to ask; the broker''s own paths, and decoding a Tenant this broker wrote (a bus event carries the Tenant a write ran under, and 5.5.15 lets a write run inside a Snapshot), go through new_internal. Matched literally: a tenant name is a case-sensitive key in the store and in SET LOCAL antares.tenant, so SNAP-index shares a keyspace with nothing and stays an ordinary client tenant (tests/tenant_isolation.rs internal_tenants_are_not_addressable_by_a_client).'
notes: 'semantics recorded at 4.14/5.5.10; this is the header binding. Local-run recipe: 414_01_03 is
  dist-ops — needs context_source_host:127.0.0.1 AND egress-allow-private, else Wait For Request hangs
  to the robot timeout.'
robot:
- '414_01'
- '5510_01'
---

6.3.14
Tenant specification
If the system implementing the NGSI-LD API supports multi-tenancy as described in clause 4.14 and clause 5.5.10, the
Tenant, to which the NGSI-LD HTTP operation is targeted, is specified as the HTTP header "NGSILD-Tenant",
whose value is the String identifying the Tenant. In case the target Tenant is the default Tenant, the HTTP header
is omitted. If the HTTP header "NGSILD-Tenant" is present in the HTTP request, it shall also be present in HTTP
response. This also applies to HTTP notifications sent as a result of subscriptions with an "NGSILD-Tenant" HTTP
header (clause 6.3.8).
