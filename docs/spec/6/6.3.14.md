---
clause: 6.3.14
title: Tenant specification
pages: '277'
status: implemented
evidence: 'negotiate.rs tenant_from (NGSILD-Tenant header, absent = default tenant) + echo_tenant on every response (incl. the NonexistentTenant 404 — 5.5.10 audit); notifications carry NGSILD-Tenant for non-default tenants (notify.rs HTTP header + MQTT metadata). TPs 414_01 (incl. tenant-less CSR forward) + 5510_01 green locally'
notes: 'audited 2026-08-12: semantics audited at 4.14/5.5.10; this is the header binding. Local-run recipe: 414_01_03 is dist-ops — needs context_source_host:127.0.0.1 AND egress-allow-private, else Wait For Request hangs to the robot timeout.'
robot:
- '414_01'
- '5510_01'
---

6.3.14
Tenant specification
If the system implementing the NGSI-LD API supports multi-tenancy as described in clause 4.14 and clause 5.5.10, the
Tenant, to which the NGSI-LD HTTP operation is targeted, is specified as the HTTP header "NGSILD-Tenant",
whose value is the String identifying the Tenant. In case the target Tenant is the default Tenant, the HTTP header
is omitted. If the HTTP header "NGSILD-Tenant" is present in the HTTP request, it shall also be present in HTTP
response. This also applies to HTTP notifications sent as a result of subscriptions with an "NGSILD-Tenant" HTTP
header (clause 6.3.8).
