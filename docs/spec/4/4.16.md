---
clause: '4.16'
title: Supporting Multiple Entity Types
pages: '97'
status: implemented
evidence: 'type is stored as an array everywhere; implicit type ADDITION on every attribute-writing path: entities.rs merge_into (5.5.12 merge patch - union, no removal, no duplicates), attrs.rs append_attrs (5.6.3 "appended types extend the type set") and update_attrs (5.6.2 same), batch update; batch upsert options=replace replaces the whole entity = the sanctioned type-removal route (5.6.8). Unit entities::clause_4_16 (union + fewer-types-fragment removes nothing + no duplicates; red seen via replace-inversion 2026-08-11). Official TPs 011_06 4/4 (incl. existing-type no-op and mixed new+existing), 004_03/004_08 (upsert replace swaps types), 005_05 - 11/11 green vs local memory broker 2026-08-11.'
notes: 'audited 2026-08-11: already implemented, no gaps - annotation + tests only. "There is no operation to remove Entity Types" holds: no handler deletes from the type array; removal = delete+recreate or batch upsert replace, both by design.'
robot:
- '004_03'
- 004_08
- '005_05'
- '011_06'
---

4.16
Supporting Multiple Entity Types
From NGSI-LD API version 1.5.1 onwards, multiple Entity Types for any Entity are supported. An Entity is uniquely
identified by its id, so whenever information is provided for an Entity with a given id, it is considered part of the same
Entity, regardless of the Entity Type(s) specified. To avoid unexpected behaviour, Entity Types can be implicitly added
by all operations that update or append attributes. There is no operation to remove Entity Types from an Entity. The
philosophy here is to assume that an Entity always had all Entity Types, but possibly not all Entity Types have
previously been known in the system. The only option to remove an Entity Type is to delete the Entity and re-create it
with the same id. Alternatively, a batch upsert with 'replace' update mode can be used, as described in clause 5.6.8.
