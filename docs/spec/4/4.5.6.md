---
clause: 4.5.6
title: Temporal Representation of an Entity
pages: '61'
status: implemented
evidence: 'antares-api/src/temporal.rs: temporal entity docs are 4.5.1-shaped with per-attribute instance
  arrays (4.5.7/4.5.8 representations, windowed in window()/present_temporal()); the Scope windows as
  the temporal representation of a Property when instance-shaped. antares-api/src/notify.rs record_temporal_change():
  a Core API scope change appends a scope Property instance carrying only the non-reified createdAt/modifiedAt/observedAt,
  with observedAt set as a copy of modifiedAt (the clause SHOULD); scope deletion appends the deletedAt
  instance (entities.rs mirror_delete_attr). Integration test: temporal_autorecord::scope_update_appends_temporal_property_instance
  (proven fallible). Extension TP 456_01 (update path, incl. no-extra-sub-properties check) + official
  020_19/020_20 (deletion path) green against the local memory broker.'
notes: 'Gap closed this pass: Core-API scope updates were never recorded into the temporal evolution (only
  deletions were). Plain-string scope (the official 5.6.11 input fixture shape) is tolerated as the unchanged-scope
  form and presented verbatim; an evolution converts it to the instance array. Instance-shaped scope on
  temporal INPUT is rejected at expansion (scope entries must be strings) — the temporal Property form
  of scope is broker-generated output, matching the official suite fixtures.'
robot:
- '456_01'
---

4.5.6
Temporal Representation of an Entity
The temporal representation of an Entity is the way to represent the Temporal Evolution of an Entity: the
Entity shall be as mandated by clause 4.5.1, but for each Property and Relationship their temporal representation shall
be provided as mandated by clauses 4.5.7 and 4.5.8 and the Scope (if present) shall be represented as a temporal
representation of a Property (clause 4.5.7) that can only have the non-reified Temporal Properties createdAt,
modifiedAt, deletedAt and observedAt as sub-Properties. This is required to represent the Scope of a Temporal
Evolution of an Entity. In case the Temporal Evolution of the Scope is updated as the result of a change from
the Core API, the observedAt sub-Property should be set as a copy of the modifiedAt sub-Property.
