---
clause: 4.5.25
title: NGSI-LD EntityMap Representation
pages: 79-80
status: partial
evidence: 'crates/antares-sql/src/store/entity_map.rs (§8.3 storage: per-row (entity_id, registration_id) materialization + expiry sweep — the entityMap member''s content); entities.rs/temporal.rs accept the entityMap param'
notes: '2026-08-10: NAMED GAP — the EntityMap JSON-LD resource (id/type/expiredBy/entityMap/linkedMaps) is not yet built or served; its whole observable surface is 5.14.1-5.14.5 + 6.3.18 (create-on-query, retrieve, update, delete). Deferred to the 5.14.x audit in clause order — same posture as 4.3.5/4.3.6.7. entityMap query param is currently accepted and ignored (placeholder).'
robot: []
---

4.5.25
NGSI-LD EntityMap Representation
The EntityMap representation is used by Context Brokers to ensure unity when querying across distributed
operations. It is an active mapping of Context Source Registrations to Entities which are relevant to an
ongoing Context Information Consumption request (see clause 4.3.6.7). The EntityMap representation shall be a JSON-
LD object containing the following members:
Mandatory
•
"id": whose value shall be the URI that identifies the attribute.
•
"type": the fixed value "EntityMap".
•
"expiredBy": a DateTime string (as defined in clause 4.6.3) for encoding a timestamp indicating the time
at which the EntityMap can no longer be used.
Optional
•
"@context": a JSON-LD @context as described in clause 4.4.
Output Only
•
"entityMap": a JSON-LD index map holding a unique list of entity ids (URIs) each of which lists the
registrations that were fired by the previous request and successfully returned data.
•
"linkedMaps": a JSON-LD index map holding a unique list of Context Source Registrations
ids (URIs) each of which lists the entityMap used by a Context Source.
