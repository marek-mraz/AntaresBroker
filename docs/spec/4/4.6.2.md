---
clause: 4.6.2
title: Supported names
pages: '80'
status: implemented
evidence: crates/antares-jsonld/src/expand.rs::valid_name (enforced in expand_attribute for attribute
  + sub-attribute names, expand_types for Entity Types); test name_grammar_rules; TP 462_01
notes: '2026-08-10: unicodeLetter/(letter|number|_)* via char::is_alphabetic/is_numeric (Unicode L/N categories);
  invalid -> BadRequestData 400. Colon-keys (prefix:name / compact / absolute IRIs) exempt from the term
  grammar — named ceiling: a malformed "pre fix:x" passes as IRI (ponytail comment at valid_name).'
robot:
- '462_01'
---

4.6.2
Supported names
Even though the JSON serialization format allows inclusion of any character in the Unicode space, NGSI-LD restricts
Entity Type names, Property names and Relationship names to the following ABNF grammar:
nameChar = unicodeNumber / unicodeLetter
nameChar =/ %x5F




  ; _
name = unicodeLetter *nameChar

•
unicodeNumber is any Unicode character that has Number as a Category [22]. With Unicode-capable regular
expression (RegEx) parsers, such a character may be matched by \p{N}.
•
unicodeLetter is any Unicode character that has Letter as a Category [22]. With Unicode-capable regular
expression (RegEx) parsers, such a character may be matched by \p{L}.
In order to avoid name clashing, names can be prefixed as specified by the following BNF grammar:
prefix = unicodeLetter *nameChar
name =/ prefix %x3A unicodeLetter *nameChar ; prefix:name

When receiving a JSON-LD object with a name (Type, Property, Relationship) including characters different than those
expressed above, implementations should raise an error of type BadRequestData.
