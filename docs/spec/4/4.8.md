---
clause: '4.8'
title: Temporal Properties
pages: 83-84
status: implemented
evidence: 'antares-jsonld/src/expand.rs:471 (observedAt must be a 4.6.3 UTC DateTime string; reified forms fail the same guard) + parse_datetime (doc comment cites 4.6.3); unit tests rejects_bad_observed_at (non-DateTime, offset, reified) + datetime_validation; createdAt/modifiedAt are stamped by ONE function for every write path that brings an Attribute in (entities.rs stamp_instances, called by stamp_new for 5.6.1/batch and by attrs.rs for 5.6.2/5.6.3): instances and their sub-Attributes at any depth, reserved members never walked; unit attrs.rs clause_4_8 an_appended_attribute_is_stamped_to_the_same_depth_as_a_created_one (red first: the append path stamped only the top instances); TP 408_01 (extension - no official TP is tagged 4_8)'
notes: 'the served representation does not depend on which operation wrote the Attribute: AppendEntityAttributes + UpdateEntityAttributes 47/47 and Consumption/Entity 297/297 green against a local memory broker. audited 2026-08-10: DateTime-only value space enforced, non-reification follows from it (NOTE 1 is informative but the SHALL on the DateTime type carries it); createdAt/modifiedAt/deletedAt are system-set (6.3.11 read path tolerates absence); expiresAt = 4.22 (audited, transient entities); lastUsedAt is Snapshots-only - staged v1.x.'
robot:
- '408_01'
---

4.8
Temporal Properties
NGSI-LD defines the following Properties of type TemporalProperty that shall be supported by implementations:
•
observedAt is defined as the temporal Property at which a certain Property or Relationship became valid or
was observed. For example, a temperature Value was measured by the sensor at this point in time.
•
createdAt is defined as the temporal Property at which the Entity, Property or Relationship was entered into
an NGSI-LD system.
•
modifiedAt is defined as the temporal Property at which the Entity, Property or Relationship was last
modified in an NGSI-LD system, e.g. in order to correct a previously entered incorrect value.
•
deletedAt is defined as the temporal Property at which the Entity, Property or Relationship was deleted from
an NGSI-LD system.
•
expiresAt is defined as the temporal Property at which the Entity, Property, Relationship,
CSourceRegistration, Subscription or Snapshot should be deleted from an NGSI-LD system.


•
lastUsedAt is defined as the temporal Property at which a Snapshot has been most recently used, i.e. when the
most recent operation has been executed on this Snapshot.
Temporal Properties in NGSI-LD shall be represented based on the DateTime data type as mandated by clause 4.6.3.
NOTE 1: For simplicity reasons, a TemporalProperty is represented only by its Value, i.e. no Properties of
TemporalProperty nor Relationships of TemporalProperty can be conveyed. In more formal language, a
TemporalProperty does not allow reification.
NOTE 2: It is important to remark that the term TemporalProperty has been reserved for the semantic tagging of
non-reified structural timestamps (observedAt, createdAt, modifiedAt, deletedAt, expiresAt), which
capture the temporal evolution of Attributes. Only such structural timestamps can be used as
timeproperty in Temporal Queries as mandated by clause 4.11.
NOTE 3: User-defined Properties whose value is a time value (Date, DateTime or Time) are defined as Property,
not as TemporalProperty, and are serialized in NGSI-LD as shown in annex C, clause C.6.
Whenever a TemporalProperty value is unknown by a registered Context Source, the Property shall be omitted
rather than sending an error response.
