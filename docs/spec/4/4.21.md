---
clause: '4.21'
title: NGSI-LD Attribute Projection Language
pages: 101-102
status: implemented
evidence: 'antares-api/src/repr.rs: parse_projection (disjunction via, AND the | orOp, nested {ProjectionTerm}
  for Linked Entity Retrieval, duplicate/empty-member/unbalanced-brace/leading-special rejections) + Repr.pick
  (strict: core members only survive when picked) / Repr.omit (childless node omits its head; with children
  constrains the linked entity) applied in repr::apply; pick/omit/attrs mutual exclusion per the clause.
  Unit repr::clause_4_21 (pipe==comma top-level and nested, rejection shapes; red seen — | was not an
  accepted character). Official projection TPs (16 across retrieve/query/temporal/subscription) — representative
  018_17/18/19 + 019_17/20 + 020_21 31/31 + extension 421_01 (pipe spelling + empty-member 400) 2/2 green
  vs local memory broker.'
notes: 'Fixed: the | or-operator was rejected outright (charset + split were comma-only). Dotted paths
  in projections are accepted as a superset (head-anchored); the ABNF here admits only AttrName + nested
  braces — dots delegate to the 6.x parameter tables that reference them. Notification pick/omit (046_42/43)
  exercised in CI (needs callback host).'
robot:
- 018_17
- 018_18
- 018_19
- 018_20
- 018_21
- 019_17
- 019_18
- 019_20
- 019_21
- 019_22
- 019_23
- 019_33
- '020_21'
- '020_22'
- '020_23'
- 028_08
- '046_42'
- '046_43'
- '046_44'
- '421_01'
- '5711_01'
---

4.21
NGSI-LD Attribute Projection Language
The NGSI-LD Attribute Projection Language shall be supported by implementations for projection parameters
(e.g. pick and omit). Its aim is to specify the Attributes to be retrieved within an Entity (or associated Linked
Entities when Linked Entity Retrieval is used). Projected Attributes are specified as a disjunction of elements,
where each element can either directly be an Attribute name, or in the case of Linked Entity Retrieval, a Relationship
name followed by an Attribute name within the Linked Entity. Since a disjunction of Attributes is a list, either a
comma or a pipe character can be used as alternative representations of the or operator. In the following, ABNF
grammar for NGSI-LD Attribute Projection Language is given.

orOp = %x7C / %x2C                                                  ; |  ,
ProjectionTerm = AttrName *1(LinkedEntityTerm) *(orOp ProjectionTerm)
LinkedEntityTerm  = %x7B ProjectionTerm %x7D





; {ProjectionTerm}

See clause 4.9 for the definition of AttrName.
EXAMPLE 1:
?pick=temperature
EXAMPLE 2:
?pick=temperature,humidity
EXAMPLE 3:
?pick=observation{temperature,humidity}
