---
clause: 4.5.23.3
title: Flattened Linked Entity Representation
pages: '78'
status: implemented
evidence: 'entities.rs collect_flat(): flattened responses append Linking + Linked Entities to one array;
  ListRelationship objectList targets now collected too (gap closed); recursion bounded by joinLevel,
  deduplicated by entity id. Integration test: linked_retrieval_joins_list_relationships (flat case).
  Official 019_13-16 + 018_10 flat cases green locally. The notification path carries the same
  representation through its own branch (notify.rs, the Subscription''s Table 5.2.12-1 join member) and
  had no test of its own while the inline branch beside it did.
  clause_4_5_23_3_flat_join_appends_the_linked_entity_to_data: the Linked Entity is a sibling in data,
  a whole Entity, with no inline entity Sub-Attribute alongside it.
  clause_4_5_23_3_a_target_already_in_the_array_is_not_repeated: two Relationships onto one URI append
  it once. clause_5_2_12_join_absent_delivers_the_linking_entity_alone: the "@none" default, as the
  control. Proven fallible -- with the flat arm unreachable the two
  4.5.23.3 cases fail and the control still passes.'
notes: ''
robot: []
---

4.5.23.3
Flattened Linked Entity Representation
With the flattened representation, the Context Broker response shall always consist of an array of Entities. This
array will consist of both Linking Entities and Linked Entities (where the retrieved Linked
Entities defined by an annotated Relationship), are appended to the array. This flattened representation allows for
batch operations (see clauses 5.6.7, 5.6.8, 5.6.9 and 5.6.10) be applied directly using the response from the Context
Broker.
An example of this representation can be found in annex C, clause C.2.2.
