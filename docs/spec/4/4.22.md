---
clause: '4.22'
title: Transient Storage of Entities and Attributes
pages: '102'
status: implemented
evidence: 'expiresAt as a settable system TemporalProperty on Entity and per-attribute-instance: expansion
  validates it as a 4.6.3 DateTime; entities.rs merge_into sets/NGSI-LD-Null-removes it (5.5.12); federation
  merge_docs does entity-level intersection/max + push_down_expires onto attributes (4.5.5); reads NEVER
  serve expired context — store/any.rs strips expired entities and attribute instances at the read boundary
  (strip_expired, all modes), the Pg arm additionally excludes expired entities in SQL; GC = sweep_expired
  ticking on ANTARES_SWEEP_SECS (main.rs, default 15 min, ETSI stack 2 s; spec-sanctioned lag) with the
  Pg/Timescale sweep in the maintenance job — which since also physically reaps expired ATTRIBUTE INSTANCES
  (maintenance.rs DELETE on attr_instances data->>expiresAt; integration test clause_4_22_expired_attr_instances_reaped,
  red seen pre-fix, run against a live timescale; reap isolated in its own tx after a live deadlock with
  concurrent ingest). 422_08 () pins the full write-site matrix: every attribute type + sub-attribute
  + per-datasetId instance, malformed-DateTime 400s at every site, CSR past-400 + lazy deletion, Subscription
  past-400 + status=expired-not-deleted (5.8.6), already-expired entity never served. Unit: merge_sets_and_null_removes_expires_at
  + merge_entity_expires_at_intersection_and_max + merge_pushes_entity_expires_at_onto_attributes. The
  stamp reaches sub-Attributes as well: 4.22 marks "a certain Entity, Property or Relationship" invalid
  and a sub-Attribute is a Property or a Relationship, so filter::strip_expired recurses through the instances
  it keeps, walking only colon-bearing (5.5.7-expanded) Attribute names so user JSON under `value`/`json`
  that spells expiresAt itself is never entered; unit strip_expired: expired_sub_attributes_leave_and_user_json_stays,
  API: tests/expiry_sub_attr_4_22.rs (an_expired_sub_attribute_is_not_served + the_parent_attribute_outlives_its_only_sub_attribute,
  both red before the recursion), TP 422_08_09. Physical removal of a sub-Attribute inside a live instance
  is not reaped by the maintenance sweep — the sanctioned lag of "clean-up processes will only run periodically";
  the read boundary is what the clause makes a SHALL. Extension TPs 422_01-422_08 (expiry across create/batch/merge/append/update
  paths, GC reap lifecycle, attr-level expiry with durable siblings, replace-clears/merge-preserves, TRoE
  history survival) 15/15 green vs local memory broker with ANTARES_SWEEP_SECS=2,. The rule reaches the
  batch paths too: 5.6.7.4/5.6.8.4/5.6.10.4 each define the batch as their single-entity clause run per
  item, and the Postgres batch statements carried no expiry predicate at all while `create`/`delete` did
  and the memory arm looped through them — so a batch delete answered success for an entity the same broker
  404s on DELETE /entities/{id}. Fixed with the predicates and pinned by pg_entity::the_batch_paths_see_an_expired_entity_the_way_the_single_entity_paths_do,
  whose sanity half asserts the expired row reads as absent first so every later assertion is about expiry
  and not absence.'
notes: 'implemented in a prior session (TPs + code predate the ledger reset); re-ran the full TP set green
  and anchored the evidence. expiresAt is storage-local (never forwarded as intrinsic — federation merge
  treats it per 4.5.5); "final deletion will always lag" honoured via read-boundary refusal + periodic
  sweep. Local-run trap: the 422 TPs need the broker started with ANTARES_SWEEP_SECS=2. live HFP demo
  (timescale, ~55k instances/min, 600s attr TTL) exposed that the SQL maintenance reaped only entities
  — reads correctly refused expired instances (oldest visible 599s) but storage grew unbounded (1.6M expired
  rows on disk); the new instance reap cleared them (2.7M->1.45M in one pass). Deletion stays a sanctioned-lag
  SHOULD; the read-boundary SHALL was never violated. The memory/file sweeper judged expiresAt by BYTE
  order while the read boundary (filter::expired_at) judges it as an instant: 4.6.3 allows a comma seconds-fraction
  and "," sorts before both "." and "Z", so a live instance or entity stamped in that form was pruned
  as expired and PERSISTED back, which the read boundary cannot undo. Both sweeper sites now call filter::expired_at;
  unit a_comma_fraction_instance_is_not_pruned_before_it_expires (both halves red before the fix) and
  a comma-form live attribute added to the shared backend contract.'
robot:
- '422_01'
- '422_02'
- '422_03'
- '422_04'
- '422_05'
- '422_06'
- '422_07'
- '422_08'
---

4.22
Transient Storage of Entities and Attributes
In some cases, it is desirable to create an Entity (or Attribute) which is only expected to be stored for a defined period of
time. Thereafter such an Entity (or Attribute) should be removed, and can be safely deleted from the context via an
automatic garbage collection process.
In this regard NGSI-LD defines the following system Property of type TemporalProperty that shall be supported by
implementations:
•
expiresAt is defined as the system temporal Property at which a certain Entity, Property or Relationship shall
become invalid and may be automatically removed from the Context Broker. For example, an Alert Entity was
created to last for 24 hours and should be removed after this period of time.
It should be noted that clean-up processes will only run periodically, and will be dependent upon the Context Broker
implementation, therefore final deletion will always lag the expiresAt timestamp to a certain extent. Furthermore,
expiresAt only applies to the local storage, i.e. the Entity or Attribute is to be deleted locally, but not on other Context
Sources or Context Broker hosting such Entity information, where no expiresAt timestamp is present. Thus expiresAt is
not considered to be intrinsic to the Entity or Attribute, but only applies to the storage of the Entity or Attribute
respectively. As it pertains to a system function (the deletion from storage after the expiration time), it is considered to
be a system attribute.
