---
clause: 4.23.1
title: Introduction
pages: '102'
status: partial
evidence: 'entities.rs order_entities: asc/desc by target value or object, dist-asc/dist-desc by haversine distance (geo::order_distance_m — closest point planar, haversine metric, the 4.10 ceiling) limited to GeoProperties with value-fallback for others; ordering is refused outside local scope (entities.rs: orderBy requires local — ''never applied to distributed operations''). Unit entities::clause_4_23 (3 tests; the pre-change code did not even accept the dist directions — compile-level red 2026-08-11). TPs 019_19/019_27/021_23 21/21 + extension 423_01 (dist-asc/desc vs orderFrom, mixed-datatype rank, bracket subitem, dist-without-orderFrom 400) 4/4 green vs local memory broker 2026-08-11.'
notes: 'audited 2026-08-11. GAP (named): string collation is codepoint order, not ICU root (und-x-icu) — and the collation parameter (4.23.3 EXAMPLES 6/7) is not implemented; upgrade path = ICU4X icu_collator. Distance uses the same in-memory metric ceiling as 4.10 near.'
robot: []
---

4.23.1
Introduction
When a Context Consumer queries for Entities retrieved from a single context broker, it shall be possible to request that
the array of Entities returned are ordered according to the values held within an Entity Attribute. Context Broker
implementations shall interpret such requests and return the array of Entities sorted accordingly.
For each Attribute name listed, one of four types of sort order can be specified:
•
Sort in ascending order by target value or target object
•
Sort in descending order by target value or target object
•
Sort by distance in ascending order from a specified GeoJSON geometry
•
Sort by distance in descending order from a specified GeoJSON geometry
When sorting by ascending or descending order, the preferred sort order for numbers and datetimes is trivial, however
for strings the default collation order shall be defined as using ICU "root" collation order ("und-x-icu") which
returns a reasonable language-agnostic sort order (see IETF RFC 6067 [36]).
Sort by distance shall be limited to GeoProperties, and shall be defined as calculating spherical distance using the
Haversine formula.
Sort ordering is never applied to distributed operations, and the defined sort ordering strategy may depend on
implementation specific configurations.
