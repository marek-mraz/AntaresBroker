---
clause: 4.3.6.6
title: Additional pre- and post-processing of extra information when contacting Context Source
pages: 42-43
status: implemented
evidence: 'crates/antares-api/src/federation.rs forward(): "accept" steers the forwarded Accept header
  (read path strips any returned inline @context before expansion, so both formats aggregate); "contentType"
  provides request + @context as the MIME type requires (ld+json inlines the context, json carries a Link);
  "jsonldContext" recompacts payload AND the term-bearing query parameters (attrs/type/geoproperty) with
  the registered context, forwards it as the Link context, forces Content-Type application/json and removes
  @context members from the payload (jsonldContext wins over contentType per its own mandate); "ngsildConformance"
  runs conformance::amend_payload (4.3.6.8) before forwarding. The jsonldContext recompaction is applied
  only to a payload it can express in the registered context: a body it cannot re-expand and a batch
  array travel in the request''s own context instead, because the Link header must name the context the
  terms are actually in (forward_context_4_3_6_6.rs). Value spaces validated at registration
  (csource.rs contextSourceInfo arm; unit test context_source_info_reserved_keys_are_validated). Extension
  TP 436_06 (4 wire tests: ld+json inline context, jsonldContext strip+Link+json, accept header,
  Attribute Fragment stays a fragment). 6.6/6.7: the Attribute the path names is translated with the
  payload (federation.rs path_attr_segment + antares_jsonld::expand_attr_name + Context::compact_iri,
  re-encoded by path_segment) and the Attribute Fragment compacted through expand_attr_fragment +
  compact::compact_instance, the reader the 5.6.4 handler itself uses and its inverse. Units
  the_path_attribute_segment_is_the_one_between_attrs_and_the_next_slash and
  an_attribute_fragment_round_trips_through_one_context_unchanged; wire tests
  an_attribute_fragment_travels_as_a_fragment_and_its_path_is_translated and
  a_delete_of_a_named_attribute_translates_its_path, both red before the change.'
notes: 'Degradation path: if the registered or request context fails to load, the forward proceeds with
  the original context rather than sending terms compacted against the wrong one (logged warn). The backwards-compatibility
  transformation itself is recorded under 4.3.6.8. A request whose path names one Attribute (6.6
  /entities/{id}/attrs/{attrName}, 6.7 with an instanceId, and the `value` sub-resource) carries that
  Attribute name in the PATH, which the Context Source expands with the advertised context: the segment
  is percent-decoded, expanded with the request context, compacted with the registered one and re-encoded
  alongside the payload, or switching the context would rename the target Attribute. The segment ends at
  the next `/`, so an instanceId and `value` stay outside it, and a name that does not expand to an
  absolute IRI keeps the WHOLE request in the original context like every other untranslatable payload.
  The 5.6.5 delete carried this in its sharpest form: no payload, so only the path names the Attribute,
  and the advertised context used to switch under it.'
robot:
- '436_06'
---

4.3.6.6
Additional pre- and post-processing of extra information when contacting
Context Source
The following key-values have a specific well-defined meaning when defined as elements within the optional array
contextSourceInfo of the CSourceRegistration.
If the key "accept" is defined:
•
the value shall be a MIME type acceptable to the Context Broker (one of: "application/json",
"application/ld+json").
•
the response from the distributed endpoint shall be returned in this defined format and if necessary, the
Context Broker shall be responsible for converting this to the desired content type when aggregating
responses to the initial request.


If the key "contentType" is defined:
•
the value shall be a MIME type acceptable to the Context Broker (one of: "application/json",
"application/ld+json").
•
the Context Broker shall provide the request and the associated @context as required by the MIME type
when distributing the request to the context source endpoint, regardless of how it was provided in the initial
request.
If the key "jsonldContext" is defined:
•
the value shall correspond to a URL reference as defined by the JSON-LD specification [2], section 3.1.
•
the Context Broker shall apply a compaction operation as defined by the JSON-LD specification [2],
section 4.1.5 over both payload and query parameters using the JSON-LD Context supplied in the value of the
"jsonldContext" key-value pair, prior to distributing the request to the context source endpoint and
forwarding with this JSON-LD context using an appropriate binding. Additionally, if a payload is defined in
the initial request to the Context Broker, the "Content-Type" of the forwarded request shall be
"application/json" and the Context Broker shall remove any @context members from the
payload prior to distributing the request to the context source endpoint.
If the key "ngsildConformance" is defined:
•
The value shall define in the form major.minor, for example 1.5.
•
The Context Broker shall apply a backwards compatibility operation over the payload (as defined by
clause 4.3.6.8) prior to distributing the request to the context source endpoint such that the forwarded payload
conforms to the specified version of the NGSI-LD specification.
