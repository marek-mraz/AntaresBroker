---
clause: 5.5.1
title: Introduction
pages: '148'
status: implemented
evidence: 'URI comparison across the broker (entity ids, datasetIds, @context URLs, registration endpoints) uses RFC 3986 section 6.2.1 simple string comparison — the section''s own baseline method ("false negatives are acceptable", 6.1). EntityId::new validates URI shape at every intake so compared values are already valid URIs.'
notes: 'audited 2026-08-11. Section 6 offers a comparison LADDER; the stronger normalizations (case/percent-encoding/scheme-based) are explicitly optional rungs — same reading recorded at the 4.9 audit (its note 4). If a conformance TP ever pins e.g. host-case-insensitive id equality, revisit here.'
robot: []
---

5.5.1
Introduction
This clause defines common behaviours for the API operations.
When comparing URIs, implementations shall follow the recommendations of IETF RFC 3986 [5], section 6.
