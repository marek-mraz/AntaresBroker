---
clause: 5.5.10
title: Multi-Tenant Behaviour
pages: 154-155
status: implemented
evidence: 'Tenant scoping: every store call is tenant-keyed (ADR-0001; tenant_isolation.rs pack). Default
  tenant when unspecified: negotiate.rs tenant_from. Tenant echoed on responses: echo_tenant (created/no_content/
  multi_status + the NonexistentTenant error path) and notification NGSILD-Tenant headers (notify.rs).
  Implicit tenant creation on create ops: store-level (memory maps / pg ensure_tenant); inventory + purge
  on the admin surface (lib.rs tenants_list/tenant_purge, tests/tenants_admin.rs). NonexistentTenant on
  non-create ops: NEW lib.rs tenant_exists_layer + AnyStore::tenant_exists (memory: any kind-map has the
  tenant; pg: tenants table) — implemented (red, tests/tenant_isolation.rs clause_5_5_10_nonexistent_tenant_on_non_create_ops
  + lib.rs tenant_header_is_echoed_and_validated). TP 5510_01 2/2 + 414_01 3/3 + 061_01 5/5 + 436_05 3/3
  green vs local memory broker. The Tenant inventory `/q/tenants` reports is the CLIENT tenants: a tenant the broker mints for its own bookkeeping (one per Snapshot, plus the reverse index and the distributed-subscription inbound index) is left out by both arms (any.rs/mem tenant_ids), so a deployment''s snapshot churn cannot bury the accounts an operator came to read, and the per-tenant routes refuse such a name besides. It is the READ that leaves them out, not the write: on Postgres the `tenants` table is also the broker''s tenant ENUMERATION — AnyStore::subscription_tenants reads it, because `subscriptions` sits outside the antares.service escape (a subscription document carries the credentials its notification is sent with), and seed_mirror and the notification sweep walk what it answers, so a Snapshot-scoped Subscription whose tenant lost its row would stop being notified after a restart (ADR-0012 makes snapshots durable). Tests antares-sql/tests/pg.rs an_internal_tenant_holds_a_row_and_is_left_out_of_the_inventory, tests/tenants_admin.rs broker_tenants_are_not_in_the_inventory_and_not_addressable (red seen).'
notes: 'NoMultiTenantSupport (501) is defined but never raised — Antares supports multi-tenancy, and the
  SHOULD applies only to systems that do not. Implicit-create exemption list extends the clause''s six
  ops with entityOperations/upsert (it creates; the clause''s list illustrates an option, not a closed
  set). Tenant-independent resources (/info/*, /jsonldContexts*) are exempt from the existence check —
  the per-tenant alias must be retrievable before any data exists, and hosted @contexts are stored tenant-less.
  Tenant string form + charset posture: 4.14.'
robot:
- '5134_01'
- '5510_01'
- '577_01'
---

5.5.10
Multi-Tenant Behaviour
If a Tenant is specified for an NGSI-LD operation, the operation shall only be applied to information related to the
specified Tenant. If no Tenant is specified, the operation shall apply to the implicitly existing default Tenant. If a
Tenant is explicitly specified, but the system implementing the NGSI-LD API does not support multi-tenancy, an
error of type NoMultiTenantSupport should be raised.
In case an operation applies to a Tenant, this information shall also be provided in the response to the operation. This
also applies to notifications sent as a result of subscriptions (clauses 5.8 and 5.11).
A Tenant is represented in form of a String. How the Tenant is specified for an API operation is protocol binding
specific. How Tenants are created, is implementation-specific.
One implementation option is to support the implicit creation of Tenants. This means that a Tenant is implicitly
created when an NGSI-LD operation for creating information targets a new Tenant; this is the case for:
•
Create Entity (clause 5.6.1).
•
Batch Entity Creation (clause 5.6.7).
•
Create or Update Temporal Evolution of an Entity (clause 5.6.11).
•
Create Subscription (clause 5.8.1).
•
Register Context Source (clause 5.9.2).
•
Create Context Source Registration (clause 5.11.2).
All other NGSI-LD operations, e.g. for retrieving, updating, appending or deleting information that target a non-existing
Tenant should raise an error of type NonexistentTenant.
If the system implementing the NGSI-LD API does not support multiple Tenants, the attempt to register a Context
Source with Tenant information in the Context Source Registration should also result in an error of type
NoMultiTenantSupport.
