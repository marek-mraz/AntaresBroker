---
clause: 5.2.3
title: '@context'
pages: '105'
status: implemented
evidence: negotiate.rs served_context (single point for the @context member on ld+json bodies — user context
  echoed where present, core context alone otherwise) used by inject_context (retrieve, notifications
  via notify.rs), the GeoJSON branch of respond and the streaming respond_list; application/json responses
  carry the Link header instead (6.3.5). Unit negotiate::clause_5_2_3. TP 018_07 (strict ld+json body
  comparison incl. @context) 6/6 green vs local memory broker.
notes: 'INTERPRETATION DOUBT: the literal sentence asks for [user, core]; the official suite pins user-context-only
  in 68 strict-compared expectations and is validated against Scorpio/Stellio — the ecosystem treats the
  core as implicit per 4.4. Antares follows the ecosystem reading; a literal-reading implementation was
  built, seen red against the suite shape, and deliberately reverted. Raise upstream as a wording clarification.'
robot: []
---

5.2.3
@context
When encoding NGSI-LD Entities, Context Source Registrations, Subscriptions and Notifications, as pure
JSON-LD (MIME type "application/ld+json"), an array (flattened to a single string if necessary), containing
a user @context where present, and the core @context (as described in clause 4.4) shall be included as a special member
of the corresponding JSON-LD Object. Table 5.2.3-1 gives a precise definition of this special member.
Table 5.2.3-1: JSON-LD @context tagged member
Name
Data Type
Restriction
Cardinality
Description
@context
URI, JSON Object, or JSON Array
See [2], section 5.1.
0..1
JSON-LD @context.
