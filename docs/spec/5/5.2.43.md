---
clause: 5.2.43
title: OrderingParams
pages: '144'
status: implemented
evidence: 'batch.rs query_doc_params ordering arm (completes the 5.2.23 named gap): orderBy String[] →
  the 4.23 keys, coordinates JSON array → orderFrom, geometry string → orderGeometry (default Point downstream);
  ordering now APPLIED on the POST query path (order_entities pre-pagination — previously only the GET
  twin ordered). orderGeometry added to QUERY_PARAMS + the temporal allowed list (was read but rejected
  by check_params — non-Point dist ordering was unusable via GET). coordinates-mandatory-for-dist enforced
  downstream ("dist ordering requires orderFrom", 4.23.3). Unit lib.rs clause_5_2_43 (red ordering was
  mapped but never applied on POST). TPs: new 5243_01 5/5 + 423_01 + 019_32 = 18/18 green vs local memory
  broker.'
notes: collation member CLOSED (query_doc_params -> collation param -> build_collator; lib.rs clause_5_2_43
  + tests/collation_4_23.rs clause_5_2_43_post_query_collation_member).
robot:
- '5243_01'
---

5.2.43
OrderingParams
This datatype represents the parameters that convey the definition used whilst ordering Entities.
The supported JSON members shall follow the requirements provided in Table 5.2.43-1.
Table 5.2.43-1: OrderingParams data type definition
Name
Data Type
Restriction
Cardinality
Description
collation
String
An ICU collation (see IETF
RFC 6067 [36]).
0..1
The Entities returned in the
payload shall be ordered
according to the collation
given. By default, it shall be
ICU "root" collation order
("und-x-icu").
coordinates
JSON Array
A JSON Array coherent with
the geometry type as per
IETF RFC 7946 [8].
0..1
It shall be
one if
orderBy
uses order
by distance
Coordinates of a Geometry
used when sorting by
distance.
geometry
String
A valid GeoJSON [8]
geometry type excepting
GeometryCollection.
0..1
The type of geometry whose
coordinates are used when
sorting by distance. By
default, it shall be a
"Point" geometry.
orderBy
String[]
Each String is an Entity
member ("id", "type",
"scope" or an Attribute
name) appended with an
optional sorting style
("asc", "desc",
"dist-asc", "dist-
desc") as per clause
4.23.
When defined, the Entities
returned in the payload shall
be ordered according to
members defined.
