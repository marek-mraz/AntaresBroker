---
clause: 5.13.4.4
title: Behaviour
pages: 246-247
status: implemented
evidence: 'contexts.rs serve_context: full content for Hosted/ImplicitlyCreated; OperationNotSupported 422 for Cached (incl. pinned core); ResourceNotFound 404 for unknown; details=true metadata for all kinds; tests clause_5_13_4_serve_errors (invert-proven) + roundtrip; TPs 053_01-053_08'
notes: 'audited 2026-08-12. 2026-08-16 (matrix 12, 053_08 fleet): serve-hit bump skipped for broker-internal fetches (x-antares-ctx-fetch marker set by the loader) — a fleet peer resolving an ImplicitlyCreated @context through the LB counts the use itself via usage_bump; the serve-side bump double-counted. Client serves still count (tests/contexts_fleet_5_13.rs internal_context_fetch_does_not_count_as_serve_hit, red-first).'
robot: []
---

5.13.4.4
Behaviour
•
If details is set to false, or details is not present, the broker shall give back the full content of the @context that
corresponds to the indicated local identifier, serving it from its internal storage, if the @context that
corresponds to the indicated local identifier is of kind "Hosted" or "ImplicitlyGenerated". It shall
give back OperationNotSupported error if it is of kind "Cached". It shall give back ResourceNotFound if
the identifier is not found.
•
Otherwise, if details is set to true, the broker shall give back metadata about the @context that corresponds to
the indicated local identifier. It shall give back ResourceNotFound error if the identifier is not found.
