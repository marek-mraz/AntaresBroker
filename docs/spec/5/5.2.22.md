---
clause: 5.2.22
title: KeyValuePair
pages: '124'
status: implemented
evidence: 'Table 5.2.22-1 (key String 1, value String 1) enforced at every KeyValuePair[] intake: subscriptions.rs
  normalize (endpoint receiverInfo/notifierInfo — value was previously accepted as any non-null JSON)
  and csource.rs (contextSourceInfo — value likewise; the four 4.3.6.6 processed keys keep their narrower
  value spaces on top). Consumption side conveys the validated pairs as headers (notify.rs, federation.rs).
  Unit lib.rs clause_5_2_22 (2 tests, red pre-fix, negative JSON types per member). TPs: new 028_10 5/5
  + 033_12 5/5, full CreateSubscription + RegisterContextSource suites 53/53 green vs local memory broker.'
notes: 'Restrictions are "Binding-dependent" — the HTTP binding conveyance itself (headers on notify/forward)
  is recorded at 5.8.6/4.3.6.5-4.3.6.6. Trap: a stale broker binary made the first Robot run pass 201
  on the negative cases — always rebuild antares-broker after crate edits before a rule-8 run.'
robot:
- 028_10
- '033_12'
---

5.2.22
KeyValuePair
This datatype represents the optional information that is required when contacting an endpoint for notifications.
The supported members shall follow the indications provided in Table 5.2.22-1. They are intended to represent a
key/value pair.
Example optional information includes additional HTTP Headers such as:
•
The HTTP Authentication Header.
•
The HTTP Prefer Header (IETF RFC 7240 [26]) used when notifying the GeoJSON Endpoint.
Table 5.2.22-1: KeyValuePair data type definition
Name
Data Type
Restrictions
Cardinality
Description
key
String
Binding-dependent
The key of the key/value pair.
value
String
Binding-dependent
The value of the key/value pair.
