---
clause: 5.5.5
title: Default @context assignment
pages: '150'
status: implemented
evidence: 'negotiate.rs parse_body (no Link, application/json → loader.core()) and request_context (GET/DELETE
  fallback), both annotated. Core precedence: loader.rs core_terms_are_protected_from_user_redefinition
  + the 5.2.3 ecosystem decision. Unit negotiate.rs clause_5_5_5_no_context_input_gets_the_core_context
  (core term maps, non-core falls to default vocab, ctx.source is exactly the Core @context; seen FAILED
  2026-08-11). TP 555_01 2/2 green vs local memory broker 2026-08-11.'
notes: 'audited 2026-08-11. The MAY (configurable default user @context) is not offered — no such config
  knob exists, by choice; the SHALL (core assignment) and core precedence are the implemented surface.'
robot:
- '555_01'
---

5.5.5
Default @context assignment
If the input provided by an API client does not include any @context, then the implementation shall at minimum assign
the Core @context to such an input. In addition, the Context Broker implementation may allow configuring a
default user @context (with default terms), to be used when no user @context is provided. The Core @context shall
always take precedence.
