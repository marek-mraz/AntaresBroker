---
clause: 5.2.33
title: EntitySelector
pages: 131-132
status: implemented
evidence: 'Table 5.2.33-1 enforced at every EntitySelector intake: batch.rs query_doc_params (POST query
  bodies — type mandatory, id String or String[] of EntityId-validated URIs [array form ADDED this audit
  — was 400], idPattern string) and subscriptions.rs normalize (same members, idPattern regex-compiled,
  id array form ADDED). "id takes precedence over idPattern" implemented in all three matchers: entities.rs
  filter loop, temporal.rs filter loop, notify.rs selector_match (which also gained the id-array form
  — an array id previously matched EVERYTHING because as_str returned None). Unit lib.rs clause_5_2_33
  (2 tests) + notify::clause_5_2_33 (matcher precedence/array), all red 2026-08-11 pre-fix. TPs: new 5233_01
  6/6 + 019_32 + 019_02_01..06 + CreateSubscription suite + 021_13 = 50/50 green vs local memory broker
  2026-08-11.'
notes: 'audited 2026-08-11. type "*" = no type predicate (Table 6.4.3.2-1 reading, implemented in entities.rs
  type_sel; the 5.5.13 localOnly interplay audits there). The same allowance on a SUBSCRIPTION selector
  ("*" is also allowed as a value; 5.2.33 scopes EntitySelector to what is "queried or subscribed to")
  was NOT implemented: subscriptions.rs expanded "*" as a term, so the stored selector carried an IRI no
  Entity carries and selector_match answered false for every change — the subscription was created 201
  and then notified nothing, with nothing logged. "*" now stays raw through normalization and satisfies
  the type predicate in antares-matcher selector_match. Red first on both halves, and TP 5233_02 red on
  the wire against an unfixed broker (the notification never arrived), green against the fixed one.
  index_keys already indexed it Broad; spec_for_subscription leaves "*" in CsrSpec.types, where it matches
  no registration — which is what Table 5.2.33-1''s "implied local scope" asks for, but by accident rather
  than by rule. 4.17 selection expressions in selectors stay
  raw and evaluate at match time (046_16 decision). EntityInfo (5.2.8, registrations) is a different type
  with its own typeless-tolerance decision — do not conflate.'
robot:
- '5233_01'
- '5233_02'
---

5.2.33
EntitySelector
This type selects which entity or group of entities are queried or subscribed to by Context Consumers. Entities can
be specified by their id, Entity Types or group of Entity IDs (as a regular expression pattern mandated by
IEEE 1003.2™ [11]).
The JSON members shall follow the indications provided in Table 5.2.33-1. id takes precedence over idPattern.
Table 5.2.33-1: EntitySelector data type definition
Name
Data Type
Restrictions
Cardinality
Description
type
String
A valid type selection string as
per clause 4.17. To indicate a
request for all Entities (with
implied local scope), "*" is also
allowed as a value.
Selector of Entity Type(s);
If type is specified as "*", implying local
scope, local scope shall not be explicitly
set to be false (clause 5.5.13) for the
execution of the corresponding operation.





id
String or
String[]
Valid URI(s)
0..1
Entity identifier(s).
idPattern
String
Regular expression as per
IEEE 1003.2™ [11]
0..1
A regular expression which denotes a
pattern that shall be matched by the
provided or subscribed Entities.
