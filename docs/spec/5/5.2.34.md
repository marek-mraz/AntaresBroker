---
clause: 5.2.34
title: RegistrationManagementInfo
pages: '132'
status: implemented
evidence: 'csource.rs normalize "management" arm (ADDED this audit — the member was swallowed by the tolerant
  reader): object required, cacheDuration ISO 8601 duration, cooldown/timeout numbers > 0, localOnly boolean.
  federation.rs FedReg compilation now honours management.localOnly (top-level spelling kept for compatibility).
  Unit lib.rs clause_5_2_34 (value spaces) + federation::management_local_only_survives_registration_compilation,
  both red 2026-08-11 pre-fix. TPs: new 5234_01 6/6, RegisterContextSource suite 34/34 green vs local
  memory broker 2026-08-11.'
notes: '2026-08-14 IOP campaign: management.timeout and management.cooldown are now HONOURED (FedReg.timeout_ms bounds the forward via a per-request deadline under the 8 s ceiling; per-registration cooldown window in egress.rs returns the automatic timeout error inside the window) — this supersedes the earlier MAY-override posture; the §16.7 host:port breaker stays timeout-only alongside. Red-first TPs IOP_EXT_ERR_01_05/06 on run-five; re-attempt behaviour pinned by ERR_01_07. audited 2026-08-11. The clause itself licenses behavioural override: "Brokers may override these
  recommendations." Antares overrides cooldown with its per-destination egress circuit breaker (egress.rs
  — failure backoff + half-open probe, the Table''s "timeout error before cooldown expires" semantics),
  timeout with the fixed 8 s per-forward deadline (federation.rs io_deadline, §16.7), and does not implement
  cacheDuration response caching. localOnly is honoured exactly (4.3.6.4 forwards carry local=true).'
robot:
- '5234_01'
---

5.2.34
RegistrationManagementInfo
This type represents the data to alter the default behaviour of a Context Broker when making a distributed
operation request to a registered Context Source. The supported JSON members shall follow the indications
provided in Table 5.2.34-1. Brokers may override these recommendations.
Table 5.2.34-1: RegistrationManagementInfo data type definition
Name
Data Type
Restrictions
Cardinality
Description
cacheDuration
String
String representing a
duration in ISO 8601
[17] format
0..1
Minimal period of time which shall
elapse between two consecutive context
information consumption operations (as
defined in clause 5.7) related to the
same context data will occur.
If the cacheDuration latency period has
not been reached, a cached value for
the entity or its attributes shall be
returned where available.
cooldown
Number
Greater than 0
0..1
Minimum period of time in milliseconds
which shall elapse before attempting to
make a subsequent forwarded request
to the same endpoint after failure.
If requests are received before the
cooldown period has expired, a timeout
error response for the registration is
automatically returned.
localOnly
Boolean

0..1
If localOnly=true then distributed
operations associated to this Context
Source Registration will act only
on data held directly by the registered
Context
Source itself (see clause 4.3.6.4).
timeout
Number
Greater than 0
0..1
Maximum period of time in milliseconds
which may elapse before a forwarded
request is assumed to have failed.
