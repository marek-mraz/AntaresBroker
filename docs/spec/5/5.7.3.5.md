---
clause: 5.7.3.5
title: Output data
pages: '206'
status: implemented
evidence: 'temporal.rs present_temporal renders the 5.2.20 Temporal Evolution (normalized/temporalValues/aggregated);
  pick/omit reduction via trepr + window attr filtering; Content-Range 206 partial window. 2026-08-31
  FIX (c9f3866): a `pick` naming only core members (5.7.3.3 admits "id", "type", "scope" or an Attribute
  name) selects no Attribute, and the has-any-requested-attribute guard read that empty selection as a
  miss — pick=id answered 404 ResourceNotFound where the clause reduces the Entity to the named members.
  The guard now skips a core-only pick, the way the query path already did (core_only_pick). Red first
  in temporal_projection_5_7_3.rs clause_5_7_3_5_a_core_only_pick_reduces_the_entity_rather_than_hiding_it.'
notes: audited 2026-08-12.
robot: []
---

5.7.3.5
Output data
A JSON-LD object representing the Temporal Evolution of the target Entity as mandated by clause
5.2.20.
If a restrictive list of Entity member names is present, every Entity within the payload body is reduced down to only
contain the defined Entity members.
If an exclusionary list of Entity member names is present, the defined Entity members listed are removed from each
Entity within the payload.
